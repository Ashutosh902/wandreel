diff --git a/server/extraction/pipeline.ts b/server/extraction/pipeline.ts
--- a/server/extraction/pipeline.ts
+++ b/server/extraction/pipeline.ts
@@ -259,0 +260 @@ function summarizeScreenshot(shot: ScreenshotAsset): Record<string, unknown> {
+    frameIndex: shot.frameIndex ?? null,
@@ -262,0 +264,4 @@ function summarizeScreenshot(shot: ScreenshotAsset): Record<string, unknown> {
+    originalWidth: shot.originalWidth ?? null,
+    originalHeight: shot.originalHeight ?? null,
+    resizedWidth: shot.resizedWidth ?? null,
+    resizedHeight: shot.resizedHeight ?? null,
@@ -871 +871 @@ async function processRetryFramesSequentially(input: {
-    for (const frame of manifest.frames) {
+    for (const [frameIndex, frame] of manifest.frames.entries()) {
@@ -876,0 +877 @@ async function processRetryFramesSequentially(input: {
+        frameIndex,
@@ -910,0 +912,4 @@ async function processRetryFramesSequentially(input: {
+      if (!input.includeVisual) {
+        // Attempt 1 OCR-only flow should not retain the raw image payload after text extraction.
+        screenshot.url = "";
+      }
@@ -939,0 +946,3 @@ async function processRetryFramesSequentially(input: {
+
+      // Ensure no data URL survives beyond the current frame iteration.
+      screenshot.url = "";
